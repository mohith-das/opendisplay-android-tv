*.iml
.gradle/
.toolchains/
/local.properties
.idea/
.DS_Store
/build
/app/build
/dist/
/captures
.externalNativeBuild
.cxx

# Local working notes.
/ESTADO_ATUAL.md
local.properties

# Release signing — the keystore itself lives outside the repo entirely
# (~/keystores/opendisplay-android/), but keystore.properties (which points
# to it and holds the passwords) must never be committed either.
/keystore.properties
*.jks
*.keystore
*.p12
*.pem
*.der
signing/

# Personal release helpers stay local.
/release.sh

# Local agent configuration.
/CLAUDE.md
/.claude/

# Local agent mirrors.
/AGENTS.md
/.agents/

# Artefatos de sessão de dev (screenshots do playwright, previews de design).
/.playwright-mcp/
/docs/icon-proposta/

# Root APKs are release artifacts, not source.
/*.apk

# Personal ADB installation helpers stay local.
/install.sh
/scripts/
